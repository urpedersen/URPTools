/**
* Split a string into a vector<string> using a char delimiter.
*/
#ifndef SPLIT_H
#define SPLIT_H

#include <string>
#include <sstream>
#include <vector>

using namespace std;

vector<string> split(string str, char delimiter);

#endif // SPLIT_H
